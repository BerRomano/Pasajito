<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pasajito">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pasajito">
        <activity android:name=".Taxi"></activity>
        <activity android:name=".Trufi"></activity>
        <activity android:name=".MetodoDePago" />
        <activity android:name=".newPassword" />
        <activity android:name=".Micro" />
        <activity android:name=".MenuPrincipal" />
        <activity
            android:name=".InicioDeSesion"
            android:label="@string/title_activity_inicio_de_sesion"
            android:theme="@style/Theme.Pasajito.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>